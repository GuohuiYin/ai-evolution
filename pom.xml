<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.1.1</version>
    <relativePath/>
  </parent>

  <groupId>com.aievolution</groupId>
  <artifactId>ai-evolution</artifactId>
  <version>0.1.0-w1-SNAPSHOT</version>
  <name>ai-evolution</name>
  <description>Java 架构师到 AI 应用架构师的实战项目（个股研究域）</description>

  <properties>
    <java.version>25</java.version>
    <spring-ai.version>2.0.1</spring-ai.version>
    <testcontainers.version>2.0.5</testcontainers.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.ai</groupId>
        <artifactId>spring-ai-bom</artifactId>
        <version>${spring-ai.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <!-- Boot 4 BOM 不再托管 Testcontainers，显式导入（2.x 起工件名带 testcontainers- 前缀） -->
      <dependency>
        <groupId>org.testcontainers</groupId>
        <artifactId>testcontainers-bom</artifactId>
        <version>${testcontainers.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <!-- 工具调用审计切面（ToolAuditAspect）：Spring AOP 运行期 -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-aspectj</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.ai</groupId>
      <artifactId>spring-ai-starter-model-deepseek</artifactId>
    </dependency>
    <!-- Embedding：SiliconFlow bge-m3，走 OpenAI 兼容协议（DeepSeek 无 Embedding API） -->
    <dependency>
      <groupId>org.springframework.ai</groupId>
      <artifactId>spring-ai-starter-model-openai</artifactId>
    </dependency>
    <!-- W3：向量库（Qdrant）。版本由 spring-ai-bom 管理 -->
    <dependency>
      <groupId>org.springframework.ai</groupId>
      <artifactId>spring-ai-starter-vector-store-qdrant</artifactId>
    </dependency>
    <!-- OpenAPI 3 文档（约定 A7）：注解生成契约 + 内嵌 Swagger UI，Boot 4 兼容线从 3.x 开始 -->
    <dependency>
      <groupId>org.springdoc</groupId>
      <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
      <version>3.1.0</version>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- Spring Boot 4 模块化：WebMvc 切片测试（@WebMvcTest/MockMvc）需独立 starter -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-webmvc-test</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- W3（约定 B1）：Testcontainers 集成测试，真实容器起 Qdrant；版本由 Boot BOM 管理 -->
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>testcontainers-junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>testcontainers-qdrant</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- W5（约定 B2）：ArchUnit 把域间依赖方向写成会失败的测试，版本独立管理（Boot BOM 不含） -->
    <dependency>
      <groupId>com.tngtech.archunit</groupId>
      <artifactId>archunit-junit5</artifactId>
      <version>1.5.0</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
      <!-- 镜像构建用 Jib 而非 Buildpacks：本机 github.com 直连不可达，Paketo 构建器无法下载
           BellSoft Liberica JRE；Jib 免 Dockerfile、走 Docker daemon 的 registry mirror 拉取
           基础镜像，可复现性同样有保障（详见 docs/adr/0002-image-build-with-jib.md） -->
      <plugin>
        <groupId>com.google.cloud.tools</groupId>
        <artifactId>jib-maven-plugin</artifactId>
        <version>3.5.2</version>
        <configuration>
          <from>
            <!-- docker:// 前缀：基础镜像直接从本地 Docker daemon 读取（已 docker pull 预拉取），
                 避免 Jib 直连 registry-1.docker.io 校验 manifest（本机不可达） -->
            <image>docker://eclipse-temurin:25-jre</image>
          </from>
          <to>
            <image>ai-evolution:${project.version}</image>
          </to>
          <container>
            <ports>
              <port>8080</port>
            </ports>
          </container>
        </configuration>
      </plugin>
      <plugin>
        <groupId>com.diffplug.spotless</groupId>
        <artifactId>spotless-maven-plugin</artifactId>
        <version>3.10.1</version>
        <configuration>
          <java>
            <googleJavaFormat>
              <version>1.36.1</version>
              <style>GOOGLE</style>
            </googleJavaFormat>
          </java>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>check</goal>
            </goals>
            <phase>verify</phase>
          </execution>
        </executions>
      </plugin>
      <!-- 测试金字塔分层：*Test 走 surefire（快，每次跑）；*IT 走 failsafe（真实容器，verify 阶段跑） -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-failsafe-plugin</artifactId>
        <executions>
          <execution>
            <goals>
              <goal>integration-test</goal>
              <goal>verify</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
