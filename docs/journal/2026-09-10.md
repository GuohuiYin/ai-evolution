# W9 周记（2026-09-10）

> PPT 机制篇约定：15 分钟，5 行——做了什么 / 卡在哪 / 下周计划。

## 做了什么

W9 缓冲周全满贯（5 项）：M2 验收归档 + docs/plans 三阶段目录体系（A13 prompt 纪律门立规）；
agent-chat-v2 反缝合规则（A13 首次执行：冰淇淋案例 0/0/1 → 2/2/2，总体 5.7/6）；
README Mermaid 分层架构图；eval 资产分层重组 + embedding/向量库两份调研论证型选型报告 + 作品集亮点清单；
M3 四候选裁决全纳入（ReAct → 查询改写 → MCP Client → 混合检索+rerank 两阶段）。
另：项目定位升格"标准答案"（三条硬约束）；规范 v1.8（A14 能力域对账 + A15 生产级差距扫描）；
能力地图建立；reranking/鉴权/SSE/轨迹评估等差距全部落位；黄金集版本台账建立。

## 卡在哪（方法论层面的两次自我修正）

1. **证据驱动的 reflexive 盲区**：多轮会话被漏掉——单轮 eval 体系产不出多轮失败证据，
   "没有证据≠没有问题，监控盲区即规划盲区"。解药：A14 能力域反推（Owner 追问触发）。
2. **扫描参照系单一**：首轮全项目扫描只做横向切面（安全/交互/可复现），reranking 从检索管线
   精排段漏网。解药：A15 双参照系（横向切面 + 纵向管线解剖 + 能力地图对照）。
   两次修正同源：清单的盲区导致按清单扫描必有盲区——方法论本身也要被评估。
3. 「12987 每个数字代表什么」生成 eval 误拒答（0/0/2）挂账，W11 查询改写时核查。

## 下周计划（W10：对话链路地基 + ReAct 骨架）

- 任务 #0：会话记忆原语（L1 内存版）+ SSE 流式 + 对话页改造（conversationId 贯穿）
- 任务 #1-2：ResearchLoop 显式骨架（TDD）+ 轨迹可观测（挂 traceId）
- 任务 #3：AgentChatService 切换 Loop 驱动，16 条生成黄金集回归不降级
- 任务 #4：M3 验收门定稿（按"可复现"口径，含 Agent 轨迹评估指标）
