# 周记 · 2026-09-04（实战 W1-W4）

1. **做了什么**：从空仓库到可演示系统——工程地基+云原生基座（W1）、DeepSeek 真实接入+OpenAPI+对话页+K8s 部署（W2）、RAG 全链路含 eval 雏形与集群冒烟（W3）、L1 补课：prompt 资产化/P0 结构化分析/模型选型实测（W4）。
2. **卡在哪**：四个坑各值一条——minikube 同 tag 镜像不覆盖导致旧代码假阳性通过（大模型会"假装"RAG 成功）；prompt 模板里 JSON 大括号与 StringTemplate 占位符冲突；SiliconFlow 余额耗尽 402 打断了 Qwen 对比实验；`deepseek-chat` 是活别名、实际指向已漂移到 v4-flash。
3. **意外收获**：v0/v1 对比实证了"prompt 工程的价值在内容保真而非格式"；长输出解析失败率 4/7 揭示了结构化输出的固有脆弱性（W5/W7 Correct 机制的立项依据）。
4. **下周计划**：W5 Function Calling（3 个 @Tool + 错误结构化 + 来源时点强制注入）+ 黄金集扩至 20 条正式过 M1 验收门（Recall@5 ≥ 0.7）+ PDF 摄入清偿 W3 欠账。
5. **一句话**：模型是商品，Harness 是护城河——这周亲手摸到了护城河的第一块砖（防幻觉闸门、红线工程化、502 语义化），也第一次用 eval 数据而不是感觉做了技术决策。
