# 2026-09-14 周记（W11）

## 做了什么

- **W11 #1 review 通过并推送**（`3285a16` + `ffb425f`，origin/main 同步）
- **W11 #2 轨迹可观测落地**（`e48d245`，TDD 先红后绿）：
  - `ResearchLoop` 每步 thought/tool/input/observation/elapsedMs 写 `research-trace` 专用 logger（tool-audit 先例），退出记录 stopReason/steps；traceId 由控制台 pattern 经 MDC 自动带出——日志即可还原完整研究轨迹
  - `LoopListener` 观测端口（onStep/onComplete，含 NONE 缺省）：SSE 轨迹推送与 W13 轨迹评估指标共用的接线点，不入侵循环本体
  - `ChatStreamPart` 新增 sealed `Trajectory` 事件，`ChatController` 穷举 switch 被编译器逼着补 case（sealed 层次设计奏效）→ SSE `event:trajectory` JSON 下发
  - 对话页轨迹折叠卡片（`<details>` 默认收起=可选展示）；delta 改走专用 text span——修掉一个潜伏 bug：`bubble.textContent=` 会覆盖已渲染的轨迹块
- 测试增量：`ResearchLoopTraceTest` 4 条（每步留痕/MAX_STEPS 退出日志/监听器顺序/三参构造兼容）+ `ChatStreamControllerTest` trajectory 事件契约 + `ChatPageTest` 页面挂钩断言；全量 verify 绿

## 卡在哪

- 无实质卡点。~~留痕一笔已知副本~~（当日即被 Owner 裁决收口，见下）

## 当日追加：A11 升级「两次即收口」（Owner 指令）

- 上午自申报的 summarize 第二处副本，Owner 当即裁决：**现在即收口，且 A11 规则一改为"两次即收口"**（不再等第三次）
- 落地（`06bef08`）：`LogSummaries` 入 infra 单点（空白归一 + 截断 100 + null→空串），ToolAuditAspect / ResearchLoop 双双改引用；`LogSummariesTest` 4 条锁语义
- ArchUnit 白名单显式登记 `tool→infra`、`loop→infra`；`Map.of` 满 10 项顺势切 `Map.ofEntries`
- 规范变更（`a79e078`）：AGENTS.md A11 规则一改文 + 版本号升 v2.1

## 明日计划

- 待 push：`e48d245`（W11 #2）→ `e1eb0c1`（文档档）→ `06bef08`（LogSummaries 收口）→ `a79e078`（A11 v2.1），共 4 个待 Owner review
- 下一步：W11 #3 接入现有通路——`AgentChatService` 切换到 ResearchLoop 驱动（三工具原样挂载），16 条生成黄金集回归不降级（A13 跑批摘要进 commit）
