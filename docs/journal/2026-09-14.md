# 2026-09-14 周记（W11）

## 做了什么

- **W11 #1 review 通过并推送**（`3285a16` + `ffb425f`，origin/main 同步）
- **W11 #2 轨迹可观测落地**（`e48d245`，TDD 先红后绿）：
  - `ResearchLoop` 每步 thought/tool/input/observation/elapsedMs 写 `research-trace` 专用 logger（tool-audit 先例），退出记录 stopReason/steps；traceId 由控制台 pattern 经 MDC 自动带出——日志即可还原完整研究轨迹
  - `LoopListener` 观测端口（onStep/onComplete，含 NONE 缺省）：SSE 轨迹推送与 W13 轨迹评估指标共用的接线点，不入侵循环本体
  - `ChatStreamPart` 新增 sealed `Trajectory` 事件，`ChatController` 穷举 switch 被编译器逼着补 case（sealed 层次设计奏效）→ SSE `event:trajectory` JSON 下发
  - 对话页轨迹折叠卡片（`<details>` 默认收起=可选展示）；delta 改走专用 text span——修掉一个潜伏 bug：`bubble.textContent=` 会覆盖已渲染的轨迹块
- 测试增量：`ResearchLoopTraceTest` 4 条（每步留痕/MAX_STEPS 退出日志/监听器顺序/三参构造兼容）+ `ChatStreamControllerTest` trajectory 事件契约 + `ChatPageTest` 页面挂钩断言；全量 verify 绿

## 卡在哪

- 无实质卡点。~~留痕一笔已知副本~~（当日即被 Owner 裁决收口，见下）

## 当日追加：A11 升级「两次即收口」（Owner 指令）

- 上午自申报的 summarize 第二处副本，Owner 当即裁决：**现在即收口，且 A11 规则一改为"两次即收口"**（不再等第三次）
- 落地（`06bef08`）：`LogSummaries` 入 infra 单点（空白归一 + 截断 100 + null→空串），ToolAuditAspect / ResearchLoop 双双改引用；`LogSummariesTest` 4 条锁语义
- ArchUnit 白名单显式登记 `tool→infra`、`loop→infra`；`Map.of` 满 10 项顺势切 `Map.ofEntries`
- 规范变更（`a79e078`）：AGENTS.md A11 规则一改文 + 版本号升 v2.1

## 当日追加二：W11 #3 通路切换（四子步全部落地）

- #3a `ToolRegistry`（`5c76c67`）：三工具按名白名单派发，失败降级为错误观察喂回模型；ToolSpec 手册供 prompt 拼装
- #3b-1 `ReactProtocolParser`（`7603f72`）：Thought/Action/Action Input/Final Answer 切分，容 Markdown 加粗与全角冒号
- #3b-2 `DeepSeekResearchModel`（`7f2c7b4`）：裸客户端（不挂工具/记忆 advisor），不合协议直通兜底
- #3c 通路切换（`b7e188e`）：AgentChatService 改 ResearchLoop 驱动；记忆手工读写（advisor 每步重复追加的坑）；SSE 逐步轨迹实时推送（Sinks + MDC traceId 交接）；ADR-0015 留痕
- **eval 两轮（A13 纪律门实战）**：首轮 4.6/6 降级被抓——模型把入参内联进 Action 行，直通兜底把协议原文当答案；修复（解析器内联 JSON 容差 + 降级 warn 日志）后复跑 **5.6/6 ≥ 基线 5.3/6**，compound/unanswerable 反超基线。报告：docs/eval/generation/w11-react-loop-regression.md

## 明日计划

- 待 push：`5c76c67`（#3a）→ `7603f72`（#3b-1）→ `7f2c7b4`（#3b-2）→ `b7e188e`（#3c+ADR）→ 本次文档 commit，共 5 个待 Owner review
- 下一步：W11 #4 M3 验收门定稿——把 m3/README 验收门草案逐条确认为可执行口径

## 当日追加三：W11 #4 + #5

- **#4 M3 验收门定稿**（`6323691`）：五条全部落成「口径+命令+阈值+落位周」；轨迹三指标口径写死（步数/冗余调用=同 tool+同 input 重复/收敛率）；混合检索启用阈值事先写死 Recall@5 ≥80%（+8pp）且失分用例无一恶化
- **#5 查询改写器**（`2c0710a`）：QueryRewriter IOP + DeepSeek 默认档 + 直通对照档（ai.rewrite.enabled）；首轮零成本、空白兜底回原 query；RagChatService 检索侧两处收口 retrieveWithRewrite
- **冒烟验收一次过**：证据 #4 场景复测——改写产出「贵州茅台酿造工艺中"12987"流程的具体含义是什么」，hits=1（原 0），回答命中五要素+溯源+免责声明
- 观察项：改写后 topScore=0.51 贴阈值 0.5——数字指代类查询仍是弱信号区，W13 混合检索时复核

## 明日计划（覆盖更新）

- 待 push：`6323691`（#4）→ `2c0710a`（#5）→ 文档档，共 3 个待 Owner review
- 下一步：W11 #6 多轮评测集——golden-set-generation 新增 multi-turn ≥6 条（先登记 changelog 再提交），eval Runner 支持同会话顺序执行，改写开/关两轮对比入报告

## 当日追加四：W11 #6 多轮评测集（验收一次过）

- 黄金集 v1.1（先登记 changelog 再提交）：16→22 条，新增 multi-turn 6 条（指代消解 3 + 追问深化 1 + 话题切换 1 + 多轮红线 1），全部围绕语料覆盖的茅台/宁德与 mock 财务数据设计；schema 增可空 `turns` 字段，单轮用例与构造器原样（向后兼容）
- `GenerationEvaluator.evaluateOne` 多轮分支：同会话顺序执行脚本、judge 评末轮；单测先红后绿
- **两轮跑批（A13 摘要进 commit）**：改写开启 **6.0/6** vs 改写关闭 **4.5/6**——严格高于，验收通过
- 关闭轮两条塌方（12987 / 第二增长曲线，均 2/6）全是"指代未消解→检索 hits=0→误称无资料"，证据 #4 病灶原案复现；话题切换不依赖改写也满分——改写是"指代消解的专用药"，不是万能药
- 报告：docs/eval/generation/w11-multi-turn.md

## 明日计划（覆盖更新二）

- 待 push：#6 代码档 + 文档档，共 2 个待 Owner review
- 下一步：W11 #7 生成 eval CI 定时回归门（GitHub Actions 定时 workflow，secrets 由 Owner 配置，不做 per-commit 阻塞）

## 当日追加五：W11 #7 生成 eval CI 定时回归门

- `EvalGate` 纯函数（TDD）：类别均分低于阈值即违约，判定逻辑进单测而不是 workflow shell 脚本；`GenerationEvalRunner` 接线 `gate.min-category-average` + `exit-after-run`（违约退出码 1 + warn 日志，本地手动跑批行为不变）
- **顺手抓到潜伏时序坑**：三个 ApplicationRunner 此前全无 @Order——摄入与 eval 的顺序靠 bean 注册顺序，本地被持久 Qdrant 掩盖；CI 起全新空库就会 eval 抢跑在摄入前。显式定序：摄入(1) → 检索 eval(2) → 生成 eval(3)
- `eval-generation.yml`：每天 UTC 21:23（北京 05:23，错峰）+ workflow_dispatch；secrets 预检 fail-fast；Qdrant v1.15.1 与本地同版；阈值 4.5/6（近期各类别 5.0~6.0，留噪声余量抓 3.x 塌方）；违约复用 open issue 追加评论不刷屏；明确不做 per-commit 阻塞
- 全量 verify 绿；验收条件中"一次成功运行记录"待 Owner 配好 secrets 后手动触发补齐

## 明日计划（覆盖更新三）

- 待 push：`52c5728`（#7）→ 文档档，共 2 个待 Owner review
- Owner 动作：配 repo secrets（AI_API_KEY/AI_BASE_URL/AI_MODEL/SILICONFLOW_API_KEY/SILICONFLOW_BASE_URL），然后手动触发一次 workflow_dispatch 补运行记录
- 下一步：W11 #8 HyDE/查询扩展评估（纯调研，结论写 capability-map）

## 当日追加六：W11 #8 HyDE/查询扩展评估（收官，裁决=不采纳）

- 调研四条理由：病灶错位（指代消解已被改写解决且量化）/ 标识符类查询本就不适用 HyDE / 词汇鸿沟收益被 W13 混合检索+rerank 覆盖（2026 证据：成熟嵌入+混合基线上只有按难度门控才划算）/ 每查询多一次生成调用与流式体验和成本账相冲
- 重评触发条件在案：W13 后 paraphrase 失分仍聚集 / 语料上万级 / 混合检索后 Recall@5 仍 <80%——重评也按 HyDE-on-uncertainty 门控模式而非全开
- 归位裁决（Owner 提点后按仓内先例）：**不写 ADR**（ADR 门槛=改变架构形态，不引入技术不改变现状，与 GraphRAG ⛔ 同类），落成 `docs/eval/selection/hyde-query-expansion-v1.md`（vectorstore-selection-v1 同款调研论证型），能力地图收敛为一行索引
- 能力地图顺手同步 W11 落地行：查询理解 ✅ / 规划 ✅ / 多轮评测 ✅ / 轨迹评估 ⚠️（留痕落地，进验收门待 W13）
- **W11 八项全部收口**；#7 唯一挂账：secrets 配置 + 一次成功运行记录（Owner 动作）

## 当日追加七：#7 挂账闭环，W11 整周收官

- Owner 配齐 5 个 repo secrets 并手动触发 `eval-generation` workflow_dispatch，**首跑通过**——#7 验收条件（workflow 入仓 + 一次成功运行记录）齐
- #8 调研报告按 Owner 提点归位 `docs/eval/selection/`（不写 ADR），`f6d2c88` review 通过已推送
- **W11 八项全部收口，无挂账**。明日开工 W12（MCP Client + 鉴权，w12-mcp-client-auth.md）
