# 2026-09-11 周记（W10）

## 做了什么

- **W10 #0 对话链路地基全部落地并推送**（`7a222fc` → `17e25d5`）：
  - #0a-1 conversationId 契约贯通（chat/rag/agent 三端点）
  - #0a-2 会话记忆 L1：`ConversationMemoryConfig` + `MessageWindowChatMemory` 定点挂载 RAG/Agent 构造器，窗口 `ai.chat.memory.window:20`
  - #0b SSE 流式：`POST /ai/chat/stream`，`ChatStreamPart` sealed（Delta/Complete），红线免责声明作最后 delta
  - #0c 对话页 SSE 流式渲染 + 会话管理
- **冒烟实证**：SSE 逐字下发 ✅；追问"其中的 12987"检索 hits=0 硬拒答——查询改写证据 #4 实锤，记入 M3 挂账清单
- **规范新增**：AGENTS.md §2.5 会话协作卫生 v2.0——按任务开新会话 + 接续 SOP（`86b690d`）
- **M3 课程计划全部成文**：W11（查询改写+评测门）、W12（MCP Client+鉴权限流）、W13（两阶段检索+M3 验收）三份周计划 + 周索引更新，新会话接续就绪

## 卡在哪

- **SSE 解析 bug**：浏览器端按行解析只取首个 data 行，多 data 行事件增量文本混入 `"data:"` 前缀——按 SSE 规范收集全部 data 行再拼接修复（`17e25d5`）。教训：协议解析别自作聪明走捷径，规范怎么写就怎么实现
- 记忆 advisor 只解决模型侧上下文；检索侧指代消解（查询改写）是独立必需项——W11 核心动机已被实锤验证

## 下周计划

- **范围调整（Owner 指令）**：ReAct 四任务移入 W11，与查询改写同周；W10 定格为对话链路地基周，今日收官
- 新会话开工口令："开始 W11" → 任务 #1 ReAct 循环骨架（TDD，`ResearchLoop` 组件）
- 之后按周索引推进 W12 → W13
- 待 push：无（2026-09-11 稍晚 Owner review 通过，`86b690d`→`7c31a7c` 共 6 个已全部推送，origin/main 同步）
- 下一步：W11 #1 ReAct 循环骨架

---

# 2026-09-11 周记（W11 开工，当日下午第二会话）

## 做了什么

- **W11 接续流程走完**：开工四件套 + 环境自检（DeepSeek/SiliconFlow key 在位；Tushare token 不在 `.env`，#3 接通路前按需补；minikube Stopped 不阻塞）
- **W11 #1 ReAct 循环骨架落地**（`3285a16`）：新子域包 `loop`（A9），7 个文件 196 行
  - `ResearchLoop` 串行骨架：模型决策 → 工具执行 → 观察回喂，轨迹显式化
  - 三条退出路径单测全覆盖：FINAL_ANSWER 收敛（观察回喂断言）/ MAX_STEPS 优雅退出（answer=null 不编造，复用 RETURN_ERROR_RESPONSE 不击穿思想）/ 首轮终答直通（零工具调用）+ maxSteps<1 fail-fast
  - IOP 双端口 `ResearchModel` / `ToolExecutor`（A10），DeepSeek 适配与三工具桥接留给 #3
  - `ModelTurn` sealed（Act/Final），编译器守穷举，同 `ChatStreamPart` 先例
- `verify` 全绿：100 单测 + 8 IT；ArchUnit 未报警（loop 零外向依赖，白名单表无需改）

## 卡在哪

- 无实质卡点。设计取舍一记：LoopResult 在 MAX_STEPS 时 answer=null 而非兜底文案——红线措辞属 chat 域职责（A12 单点化），loop 保持纯净，#3 接线时由调用方组织话术

## 明日计划

- 待 push：`3285a16`（W11 #1，待 Owner review）
- 下一步：W11 #2 轨迹可观测——每步 thought/action/observation 挂 traceId 进日志（复用 W8 四级留痕），SSE 通道推送轨迹
