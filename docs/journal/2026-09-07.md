# 周记 · 2026-09-07（W6：MCP 协议贯通）

- **做了什么**：MCP 协议精读笔记（docs/notes/mcp-protocol.md）；Streamable HTTP 暴露三工具（mcp 子域零业务代码，`Registered tools: 3`）；Inspector 官方客户端真实调通全链路；ADR-0009；验收脚本 docs/scripts/mcp-smoke.sh 入库；默认端口统一 18080（IDE 占用 8080 的冲突根因治理）。M2 验收门第一项证据拿到。
- **卡在哪**：Codex CLI 非交互验证失败——本机直连 chatgpt.com 被区域限制（403），与 MCP 实现无关；额度+网络恢复后在 Codex 桌面端补验（config.toml 已配好）。
- **学到**：MCP 是能力分发协议而非又一个 Function Calling API（Step1 笔记第 2 节）；两层错误语义实证——参数校验失败走 `isError:true` 而非 HTTP 500；A10 面向接口的红利兑现：换一种暴露方式，业务代码零改动。
- **下周计划（W7 安全/成本）**：注入攻击实验（MCP 端点现无鉴权，是真实靶子）、McpExceptionHandler 错误脱敏、prompt caching 成本优化、对话接口统一路由裁决（/ai/chat vs /ai/agent）。
- **待办追踪**：Codex 桌面端补验 ⏳；黄金集负例分层（W8）。
