# 周记 · 2026-09-08（W7：安全 / 成本）

- **做了什么**：红队基线（10 用例：应用层 4 + MCP 面 6 + 间接注入 2，docs/security/w7-redteam-baseline.md）；P0 双修复（MCP 堆栈脱敏走 Jackson mixIn、工具入参上限 2000）；P1 红线服务端告警（RedLineGuard + AOP，只告警不拦截，ADR-0011）；token 成本账本（TokenUsageAdvisor 全局挂载）；DeepSeek prompt 缓存命中提升为一等观测键。
- **卡在哪**：①修复点定位弯路——@RestControllerAdvice 对 MCP 传输层无效（异常不经过 DispatcherServlet 链），改序列化层才成；②框架未映射 DeepSeek cachedTokens，原生字段兜底才看见 93% 命中率。
- **学到**：先攻击后防御让每行安全代码都有威胁模型编号可溯（M1/M3）；告警系统的第一设计约束是误报控制（告警疲劳比漏报致命）；Agent 通路一次提问=两次模型调用，成本倍增效应只有记账后可见。
- **下周计划（W8 评估周）**：黄金集扩到 30-50 条（含 12987 问法敏感性 case、负例分层）、检索质量回顾（规则路由漏检率用真实语料复盘）、成本账汇总成报告。
- **待办追踪**：Codex 桌面端补验 MCP ⏳；REDLINE_HIT 真实命中数据积累中（决定后续是否硬拦）。
