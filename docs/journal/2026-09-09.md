# W8 周记（2026-09-09）

> PPT 机制篇约定：15 分钟，5 行——做了什么 / 卡在哪 / 下周计划。

## 做了什么

W8 评估周全满贯：CVE-2026-59318 fail-fast + 工具调用上限（测试锁定）；知识库增量摄入（SHA-256 manifest）；
prompt/response 日志可观测（开关受控）；检索黄金集 39 条基线（Recall@5 72% 过门）；
生成黄金集 16 条 + LLM judge 三维评分（judge 锚定 5/5）；few-shot A/B 实验（49:49 打平，裁决不启用）；
一页成本账（57 次实测调用 ¥0.31，单次问答 ¥0.0054）。

## 卡在哪（本周素材密度极高）

1. **TokenTextSplitter 静默丢弃短文本**（minChunkSizeChars 默认值）——mock 只验证交互看不见分块为空，
   换手写 fake 才暴露；已加零分块告警。教训：验证内容用 fake，验证协作用 mock。
2. **manifest 跨测试轮次复用**导致 IT 在新 Qdrant 容器里"跳过摄入"——增量机制正常工作的反向证明，
   测试夹具没跟上语义变化。
3. **评估逮住真缺陷**：agent 通路把年报"其他业务"数据缝合成"冰淇淋业务占比"答案——
   比编造更隐蔽的幻觉（每次工具调用都合法，结论是拼的）。列 W9：agent prompt 反缝合规则。
4. few-shot 在规则就位后边际收益为零——与 P0 结论递进，L1"收益递减"实证链闭环。

## 下周计划（W9 缓冲周 + M2 验收）

- M2 验收门复核：MCP 调通 ✅ / eval 一键可跑 ✅ / 成本账 ✅ → 材料归档
- agent prompt 反缝合规则 + 回归用例（W8 遗留）
- 作品集整理 + README 架构图；W10 起 M3 最小研究 Loop（候选：MCP Client 接外部工具、混合检索、查询改写）
