# 2026-09-17 周记（W13）

## 做了什么

- **W13 开工**：#0 设计稿闸门——ADR-0017 检索两阶段化设计（recall-top-k=20 小语料适配 /
  rerank bge-reranker-v2-m3 走 SiliconFlow / sparse 选 B 诚实标注轻量近似 / RRF k=60 配置化 /
  hybrid·rerank 默认关数据裁决 / rerank 故障降级 / 成本账一页账），Owner 评审通过转已接受
  （`5325b3e` 初稿 → `59ba48c` 业界标尺修订 → `bb0bd65` 收口）
- **W13 #1 chunk-size 对照实验，三轮全量回归落地**（`bb36298`）：
  - **实测坑先行**：`KnowledgeBaseIngestor` 增量判据只看文件 SHA-256，换 chunk-size
    不触发重摄入——不删 `knowledge_base` 集合 + manifest 就会拿上一档旧块评新档。
    三轮每轮跑前清库清清单，摄入日志核实"重写 3 跳过 0"后才认数据
  - **结果：三轮完全一致**——400/800/1200 正例 Recall@5 均 21/29（72%）、负例拒答率
    均 7/10、失败集合 11 条完全相同、分类别计数逐格相同。分块数 523/303/211 证明
    参数确实生效（排除"没改上"的假阴性）
  - **无差异的机理**：两个小 md（约 280 中文字）在三档下都是单块，chunk-size 实际只切
    年报 PDF；而失败用例几乎全部期望命中 .md（12987/literal/指代），粒度对它们零影响。
    与 W8 基线判读互证——失分是向量语义盲区，要靠混合检索（#2）而非调分块
  - **裁决：维持 800**（现值即最优，改值零收益；400 档分块 +72% 徒增 embedding 成本）。
    报告入 docs/eval/retrieval/，changelog 以 v3 对照实验档案登记，能力地图分块行 ✅
  - **挂账当日结案**：manifest 判变键不含分块参数——当日 `2a65175` 已并入参数哈希（见追加一）

## 卡在哪

- 无阻塞。三轮数据一致性高到可疑，已用分块数（523/303/211）+ 摄入日志双重核实非假阴性

## 当日追加一：#1 挂账当日结案（review 后 Owner 指令）

- **TDD 落地**（`2a65175`）：manifest 顶层携带 `chunkSignature`（分块参数元组 SHA-256，
  格式不自描述——签名只回答"变没变"）；签名不符即全量重建，旧块按 manifest 记录的
  chunkIds 逐文件清理，手工清库步骤从此消失。旧版平铺格式清单按"损坏=空清单"自愈，
  首轮全量重建后自动写成新格式
- **测试**：新增 4 条——chunk-size 变更触发全量重建（旧块删除+重写）/ 同参数保持增量跳过 /
  签名 round-trip / 旧格式判空重建；rag 域 15 条全绿
- **真实冒烟**：旧格式 manifest 首启"重写 3 跳过 0"（迁移重建），二启"全部未变更零
  embedding 调用"——双向行为与单测一致
- 对照实验报告挂账行、计划表 #1 条目同步结案

## 当日追加二：W13 #2 混合检索（数据裁决 +21pp 达标，默认启用）

- **TDD 五切片**：`c68cd12` RRF 融合器（排名倒数和，k 配置化，同分 ID 字典序可复现）→
  `c7b5921` 摄入文本规整化（数字/字母与 CJK 粘连插空格，版本并入 manifest 判变键——
  追加一的挂账修复当日就兑现价值：规整化触发自动全量重建，零手工清库）→
  `b0ad55c` sparse 覆盖率裁决（代号全中放行/假代号一律拒/纯中文二元组 ≥0.5）→
  `6d2d017` Qdrant sparse 召回路（MatchText 候选网+应用侧裁决，故障降级空路不击穿 dense）→
  `35d6fa4` 检索器接线（hybrid 开关/Optional 注入/融合后截回 topK=5 线上口径）
- **probe 驱动排坑**（设计稿未预见，ADR-0017 已补实施附记）：multilingual 分词器粘连
  数字与中文（12987流程 单 token）→ 规整化解；MatchText 严格 AND 无法服务自然语言问句 →
  降级为候选网+应用侧裁决；纯数字单 token 查询被分词器丢弃 + "茅台" 高频二元组洪泛
  候选网（50 上限全被 PDF 页挤满）→ `d906974` 标识符兜底全扫
- **数据裁决**：hybrid 关 72%（规整化语料 dense 基线，与规整化前三轮完全一致——
  规整化对 dense 零扰动）→ hybrid 开 **93%（27/29，+21pp ≥ 5pp 达标线）**，
  负例拒答率 7/10 零回归；normal/paraphrase/literal 三类全满分，W8 失败模式一
  （数字代号）模式二（原文片段）全击穿；残余 boundary 指代 2 条留 #3 rerank
- **敏感性收口**：rrf-k 30/60/100 无差异锁 60；recall-top-k 20/40 无差异定稿 20
- `5f31315` 开关翻 true（数据达标才启用，同 few-shot 裁决机制）

## 明日计划（覆盖更新）

- 待 push：`5f31315`（hybrid 启用）+ 本次文档档（报告/changelog/ADR 附记/计划表/能力地图）——待 Owner review
- 下一步：W13 #3 Reranking 精排（TDD）——召回 topK 放大已就位（recall-top-k=20），
  bge-reranker-v2-m3 走 SiliconFlow /rerank；故障降级回单阶段（ADR-0017 已登记）；
  成本账一页账（reranker-selection-v1.md，usage 字段实测计价）
