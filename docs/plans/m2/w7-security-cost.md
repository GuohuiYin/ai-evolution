# W7：安全 + 成本横切面

> 补录于 2026-09-10（当周未单独立档，自 commit 记录与阶段概要回填要点）。
> 核心交付：红队基线 + 双修复 + 红线告警 + token 账本。

## 当周交付

- **红队基线 10 用例**（docs/security/w7-redteam-baseline.md）：注入/越权/超长输入等对抗面建档立卡
- **P0 双修复**：MCP 错误堆栈脱敏（Jackson mixIn，内部异常不外泄）+ 工具入参上限 2000 字符（成本攻击防线）
- **RedLineGuard**：红线触发服务端告警（ADR-0011：第一版只告警不拦截——金融场景误拦截的代价比告警大）
- **TokenUsageAdvisor**：token 账本一等公民，cache_read 独立键——DeepSeek 缓存命中率可观测（实测 93% 场景存在）
- W7 周记归档

## 验收回看

为 W8 的两件安全加固（CVE fail-fast / 调用上限）铺了语义基础：W7 建"看得见"，W8 补"拦得住"。
