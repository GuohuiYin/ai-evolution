# W10 计划：对话链路地基（会话记忆 + SSE 流式）

> 对应 M3 阶段索引 W10 行。
> **范围调整（2026-09-11 Owner 指令）**：原 W10 的 ReAct 骨架任务（#1-4）移至 W11，与查询改写同周
> （改写本就是 Loop 的前置增强，证据 #4 已实锤）。W10 定格为对话链路地基周，#0 完成即收官。

## 背景（为什么做）

A14 对账（2026-09-10）发现双盲区：多轮会话无记忆（每次请求无状态）、交互无流式（整段等待）。
二者是 ReAct 研究 Loop 的前置地基——Loop 的轨迹推送依赖 SSE，多步研究依赖跨轮上下文。
本周先把地基打实；显式 ReAct 循环的动机与任务已随范围调整移入 W11 计划文档。

## 任务分解

| # | 任务 | 验收条件 | 状态 |
|---|---|---|---|
| 0 | **对话链路地基**（A14 对账 + 2026-09-10 扫描补入）：① 会话记忆原语——Spring AI `MessageChatMemoryAdvisor` 内存版，`conversationId` 贯穿 `/ai/chat` API 与对话页；② SSE 流式输出——替代整段等待（ReAct 轨迹推送的前置，W6 已学概念本周致用） | 同会话追问"它呢？"可见上一轮；新会话互不影响；对话页逐字流式渲染；持久化记忆挂起 W14+ | ✅ 2026-09-11（`7a222fc`→`17e25d5`；冒烟通过，顺带实锤查询改写证据 #4） |
| 1-4 | ~~ReAct 循环骨架 / 轨迹可观测 / 接入现有通路 / M3 验收门定稿~~ | — | ↪ 移至 W11（2026-09-11 Owner 指令，见 w11-query-rewrite-eval-gate.md） |

## 明确不做（防范围蔓延）

- 不做 ReAct 显式循环（已移 W11）
- 不做持久化记忆（L2 挂起 W14+，见 M3 索引页会话记忆路线）
- 不改检索侧任何参数（混合检索是 W13 的事）

## 顺序与节奏

仅 #0 一项，三个子步（契约贯通 → 记忆挂载 → SSE+对话页）已于 2026-09-11 全部完成并推送，
本周收官。ReAct 主线接续见 W11。
