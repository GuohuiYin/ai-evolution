# Reranker 选型一页账（v1）

> 2026-09-17，W13 #3。口径（ADR-0017）：官方单价 × 实测用量，对照场景 = W13 黄金集全量回归一次。
> 用量来源：rerank 开轮回归日志 `stage=RERANK usage` 逐条汇总（30 次调用实录，非估算）。

## 选型结论

锁定 **BAAI/bge-reranker-v2-m3（SiliconFlow `/rerank` 端点）**——当前最佳开源多语 reranker
（278M，ADR-0017 已裁定）；复用 `spring.ai.openai.*` 同一份 SiliconFlow 凭证，零新增密钥面（A5）。

## 实测用量（黄金集全量回归一次，39 条查询）

| 指标 | 值 | 说明 |
|---|---|---|
| rerank 调用次数 | 30 | 9 条零召回查询短路（recallHits=0 不打计费电话，代码行为） |
| 输入 token 总量 | 103,770 | 逐条 `meta.tokens.input_tokens` 汇总 |
| 平均每条查询 | 3,459 token | recall-top-k=20 个候选 + query |
| 故障/降级 | 0 次 | 全轮无降级（降级路径单测锁定，本轮未触发） |
| 端到端延迟增量 | 约 +0.4s/查询 | 39 条全量回归墙钟对比（hybrid 轮 ~95s vs rerank 轮 ~110s），粗估口径 |

## 费用（官方单价 × 实测用量）

| 口径 | 单价 | 单次全量回归费用 |
|---|---|---|
| BAAI 命名空间（当前调用形态） | 免费（SiliconFlow 价格页 BAAI 系列免费档，2026-09-17 查证） | ¥0 |
| Pro 命名空间（保守上界） | ¥0.07 / M tokens（价格页 Pro/BAAI/bge-reranker-v2-m3） | 0.104M × 0.07 ≈ **¥0.007** |

即使按 Pro 上界：每查询约 ¥0.00024，日均千次查询月成本约 ¥7——费用不构成启用/不启用的裁决因素。

## 裁决记录（W13 #3）

费用可行但**收益裁决不达标**：黄金集 Recall@5 开/关均 27/29（93%），+0pp < 5pp 启用线
——rerank 保持默认关，实现与配置保留。重评触发：召回侧格局变化（新 sparse 升级/语料上量）、
黄金集扩编后排名类用例增多、或线上出现"捞回来排错位"实证。数据详见
[w13-rerank-eval](../retrieval/w13-rerank-eval.md)。
